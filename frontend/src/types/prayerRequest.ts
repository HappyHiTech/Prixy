export type PrayerRequest = {
  id: string;
  // userId: string;
  // recipientId: string;
  // categoryId: string;
  requestText: string;
  // status: "inbox" | "active" | "answered";
  // frequencyType: "one_time" | "recurring";
};
